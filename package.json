{
  "name": "open-hive",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "packageManager": "npm@11.6.2",
  "devDependencies": {
    "turbo": "^2",
    "typescript": "^5.7"
  }
}
